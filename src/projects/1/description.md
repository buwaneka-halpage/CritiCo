Here are a few **portfolio-ready descriptions** you can copy-paste (simple wording, but still “industry-level”), plus a **tech stack line**.

### Option A (short portfolio paragraph)

Built a real-time desktop soundboard for call-center style conversations that lets agents trigger pre-recorded phrases with global hotkeys while still speaking naturally through the mic. The app mixes microphone + sound clips into one live call stream with very low latency, includes local monitoring, and keeps audio levels consistent for a clean, professional sound.

**Tech stack:** C++20, Qt 6 (QML UI), real-time audio engine, RNNoise (noise reduction), SpeexDSP (gain/AGC), LUFS loudness normalization, FFmpeg (format support + editing), OpenAI Realtime/WebSocket (live transcription), OAuth 2.0 + cloud sync, CI/CD + automated packaging/releases.

---

### Option B (resume-style bullets)

* Developed a low-latency **soundboard + microphone mixer** desktop app in **C++20 + Qt 6/QML**, enabling instant phrase playback via **global hotkeys** during live calls.
* Implemented real-time audio processing: **RNNoise** noise suppression, **SpeexDSP** gain control, and **LUFS-based loudness normalization** for consistent output volume.
* Added productivity tools including a **teleprompter** and **live transcription** (OpenAI Realtime/WebSocket), plus clip editing and WAV/MP3/OGG support via **FFmpeg**.
* Delivered production features: **secure login (OAuth 2.0)**, **cloud-synced profiles**, and automated **CI/CD, installers, and release updates**.

---

### Option C (one-liner / headline)

**Real-time Soundboard + Mic Mixer (C++/Qt):** hotkey-triggered audio clips + live mic mixed into a single call stream with noise reduction, loudness leveling, transcription, and cloud-synced profiles.

---

If you tell me your target platform(s) (Windows only vs Windows/macOS), I can tighten the wording to match exactly—otherwise the above stays accurate and safe without over-claiming.